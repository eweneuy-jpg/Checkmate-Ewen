# Generated by `npm run build:openapi` (scripts/buildOpenApi.ts), which writes
# JSON.stringify(spec, null, 2) — two spaces, while .prettierrc sets useTabs.
# Formatting it here would be undone by the next build and churn ~14k lines.
openapi.json
